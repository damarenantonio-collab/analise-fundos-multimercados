[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fundos"
version = "0.1.0"
description = "Análise de fundos multimercados brasileiros"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
